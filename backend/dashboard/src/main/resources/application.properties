# === Alpha Vantage temporary API integration ===
alpha.vantage.enabled=${ALPHA_VANTAGE_ENABLED:false}
alpha.vantage.apikey=${ALPHA_VANTAGE_API_KEY:RUD59ZNA5UWUZH3M}

# === Zerodha (kept ready for later) ===
zerodha.enabled=${ZERODHA_ENABLED:false}
zerodha.apikey=${ZERODHA_API_KEY:}

# Redis
spring.redis.host=${REDIS_HOST:redis}
spring.redis.port=${REDIS_PORT:6379}

# Disable Testcontainers when using compose-based redis
spring.testcontainers.enabled=${SPRING_TESTCONTAINERS_ENABLED:false}
